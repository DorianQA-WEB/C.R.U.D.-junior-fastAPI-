# CRUD Junior — Учебный проект на FastAPI

Это учебное веб-приложение, реализующее систему управления товарами, категориями, отзывами и корзиной. Проект построен на **FastAPI**, **SQLAlchemy (асинхронно)** и **PostgreSQL/SQLite**, с поддержкой JWT-аутентификации и ролевой моделью доступа.

---

## 📦 Функциональность

### 🛍️ Товары и Категории
- Создание, чтение, обновление и удаление товаров и категорий.
- Иерархические категории (родитель-ребёнок).
- Фильтрация по активным/неактивным товарам..
### 👥 Пользователи и Роли
- Регистрация и аутентификация.
- Роли: `buyer`, `seller`, `admin`.
- JWT-токены для защиты эндпоинтов.

### ✍️ Отзывы
- Покупатели могут оставлять отзывы на товары.
- Оценка от 1 до 5.
- Мягкое удаление отзывов (только для администраторов).
- Автоматическое обновление среднего рейтинга товара.

### 🛒 Корзина
- Пользователь может добавлять, изменять и удалять товары в корзине.
- Автоматический расчёт общей стоимости и количества.
- Поддержка пагинации и фильтрации.

---
## 🛠️ Технологии

- **FastAPI** — для создания REST API.
- **SQLAlchemy 2.0+** — ORM (асинхронный режим).
- **Pydantic** — валидация данных.
- **JWT** — аутентификация.
- **Alembic** — миграции базы данных.
- **SQLite / PostgreSQL** — база данных.
- **Uvicorn** — ASGI-сервер.

---

## 🚀 Запуск проекта

### 1. Клонируйте репозиторий
```
bash git clone https://github.com/yourusername/CRUDjunior.git cd CRUDjunior
```


### 2. Создайте виртуальное окружение и установите зависимости
```
bash python -m venv .venv source .venv/bin/activate # Linux/Mac
```
или
```
.venv\Scripts\activate # Windows
pip install -r requirements.txt
```

### 3. Настройте переменные окружения
Создайте файл `.env` в корне проекта:
```
env SECRET_KEY=your-super-secret-jwt-key-here ALGORITHM=HS256 ACCESS_TOKEN_EXPIRE_MINUTES=30
DATABASE_URL=sqlite+aiosqlite:///./data/shop.db
```

Или для PostgreSQL:
```
DATABASE_URL=postgresql+asyncpg://user:password@localhost/dbname
```

> 💡 Сгенерируйте `SECRET_KEY`:
> 
```
python -c "import secrets; print(secrets.token_hex(32))"

```
### 4. Создайте папку для базы данных
```
bash mkdir data
```

### 5. Примените миграции
```
bash alembic upgrade head
```

### 6. Запустите сервер

```
bash uvicorn app.main:app --reload --host 0.0.0.0 --port 8000
```

---

## 🌐 Документация API

После запуска перейдите по адресу:
- **Swagger UI**: `http://127.0.0.1:8000/docs`
- **ReDoc**: `http://127.0.0.1:8000/redoc`

---

## 🧪 Примеры запросов

### Регистрация пользователя

http POST /auth/register Content-Type: application/json
{ "email": "user@example.com", "password": "securepass123", "role": "buyer" }

### Вход и получение токена

http POST /auth/login Content-Type: application/x-www-form-urlencoded
username=user@example.com&password=securepass123

### Добавление товара в корзину

http POST /cart/items Authorization: Bearer <your_jwt_token> Content-Type: application/json
{ "product_id": 1, "quantity": 2 }

---

## 📁 Структура проекта
CRUDjunior/ ├── app/ │ 
                     ├── main.py # Точка входа │
                     ├── models/ # ORM-модели │ 
                     ├── routers/ # Роутеры │ 
                     ├── schemas/ # Pydantic-схемы │ 
                     ├── auth.py # Аутентификация и JWT │ 
                     ├── db_depends.py # Зависимости БД │ 
                     └── database.py # Настройка БД 
                     ├── migrations/ # Alembic миграции
                     ├── data/ # Файл базы данных (при использовании SQLite)
                     ├── .env # Переменные окружения 
                     ├── requirements.txt # Зависимости
                     └── README.md # Этот файл

---

## 🧹 Миграции

Создание новой миграции:
```
bash alembic revision --autogenerate -m "Add cart table"
```

Применение миграций:
```
bash alembic upgrade head
```
## 🤝 Автор

- **Владимир**: [Владимир]
- **Email**: qa.doriangrey@gmail.com

---